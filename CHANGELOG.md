# Approved stages

One row per edit signed off. Long-form metrics come straight from the `build`
output; short-form rows record the parameters the render was approved at, since
those are what reproduce it.

Return to any stage with `git checkout <tag>`.

## Crypto Wiki — long form

| video | source | runtime | voice | notes |
|---|---|---|---|---|
| `crypto-exchanges-long` v2 | `posts/understanding-crypto-exchanges` | 4:23 | `mia` (candidate) | **Third long-form video, second on the demand ranking at 1,038 views.** 1920x1080, 6 chapters (shortest 28s), `night-drift` music. 55 sentences across 51 shot slots. Angle takes the custody line the article buries in a Cons bullet — you hold zero of the keys — and puts it in the first thirty seconds, so the CEX/DEX taxonomy arrives as the answer rather than as an agenda. Six drawn beats, six silhouettes: `stat`, `logos`, `compare`, `grid`, `quote`, `checklist`, `steps`. **v2 is the review round and it changed nine things.** Seven became engine changes and are in the commit for them; two are script rules worth repeating here. **Pauses have to be written** — every sentence at the default 0.34 is what "monotone" means, and the gap convention (0.34 inside a thought, up to 2.40 for a two-phase beat) cost 16s of runtime and is the single biggest quality change in the cut. **A website screenshot is an infographic**: `bitfinex-ui.png` measures L46 and is a bright teal marketing page with unreadable small type — the luma box screens photographs, not UIs, and both exchange screenshots came back out in favour of the site's own app photo. Also: "Binance" respelled `Bynanse` for espeak (`baɪnˈæns` is bye-NANCE, which is wrong), "BTC / USD" spoken as the three letters, and the checklist switched to `flow` so each tick lands on the check that earns it. Thumbnail hand-placed at `crop_at=(0,0)`: the face cascade returns a box already running off this source, so the scorer cannot discriminate on it. |
| `crypto-saylor-treasury-long` v1 | `crypto-ogs/michael-saylor` | 3:51 | `mia` (candidate) | **Second long-form video, approved and uploaded.** 1920x1080, 7 chapters (all clear the 10s minimum), -14.2 LUFS, -0.9 dBTP. 42 sentences / 646 words across 48 shot slots, 6 held with `None`. Keeps the 62s short's angle — a boring software company emptied its bank account — and spends the extra three minutes on what the short could not hold: the financing mechanics (a `compare` beat, convertible debt against at-the-market share sales), the 2022 step-down and the 2025 rename to Strategy, and the bear case as its own chapter. Closes on a governance question, not a verdict, and carries the Commons CC BY-SA block plus an explicit no-advice line. **Three faults were found by looking at frames, none visible in the logs.** Six site images were far too bright for the palette (`investing.jpg` L196, `stock-trader.jpg` L170) against the pilot's ceiling of L82, and were swapped for dark equivalents. `ftx-collapse.jpg` under the leverage line read as exchange fraud, which is a different failure from debt-funded volatility. `one-coin.jpg` is OneCoin — Ruja Ignatova's fraud — and beside Saylor it is an accusation the script never makes. Two engine fixes came out of this cut: the photo-border watermark dodge, and the subpixel Ken Burns. Thumbnail overrides the scorer: `saylor-speaking.jpg` at +0.91 busy with `side="left"`, taken over two clean-scoring sources because they are Saylor twenty years ago. |
| `crypto-satoshi-proof-long` v1 | `what-it-actually-takes-to-prove-someone-is-satoshi-nakamoto` | 2:47 | `mia` (candidate) | **First long-form video, approved for upload.** 1920x1080, 7 chapters, -14.2 LUFS, -0.6 dBTP. 39 sentences across 33 shots (6 held with `None`). Structure is a continuous arc — hook, reframe, deep dive, twist, mirror, echo — not the numbered chapter deck of the first three cuts. No burned captions; the full transcript ships as SRT. Six drawn beat types (`chapter`, `checklist`, `stat`, `compare`, `quote`, `bars`), six screened Pexels clips, `pulse` music bed generated by `core/music.py`, riser/impact/whoosh on section turns, push transitions at 0.34s, subscribe sting screen-blended over the last 7s. Thumbnail: headline left, subject panned right so the Bitcoin clears the type. |

## Crypto Wiki — shorts

| video | source | runtime | voice | notes |
|---|---|---|---|---|
| `crypto-exchanges-short` v5 | `posts/understanding-crypto-exchanges` | 45.0s | `mia` | The fourth logo tile's payload had already been swapped from Hyperliquid to PancakeSwap (v4, once the site gained a real page for it), but the narration line naming the four exchanges was never updated - the voice still said "Hyperliquid" over the PancakeSwap tile. Caught by the user watching the actual render, not by anything in this pipeline. Fixed the sentence; no other change. **The already-uploaded YouTube video still has the old audio** - this render has not been re-uploaded, since upload is deliberately not something this tool does. |
| Correction, 2026-08-19 | - | - | - | The row above overstated what shipped. Only `crypto-exchanges-long` went through the site registry and Share Video workflow - the short never should have, and its registry entry, poster and social posts were a mistake, reverted where revertible (registry, poster) and left as-is where not (the Telegram/Facebook posts, which cannot be unsent - ids kept in `HANDOFF.md`). `render_video_poster` is gone; posters are fetched from YouTube's own `maxresdefault.jpg` (`fetch_video_poster`), never rendered locally. |
| `crypto-exchanges-long` v3, `-short` v4 | `posts/understanding-crypto-exchanges` | 4:24 / 44.8s | `mia` | **Both went live 2026-08-19.** Long IDs to `PjKnbA4ORaw`, short to `ip9moTeFR7U`, both still unlisted on YouTube. Thumbnail: the long's subject slid 16% left with the vacated band faded to black (`render_thumb(shift=0.16)`) rather than zoomed, after the scorer proved unable to clear his face of the type at any zoom on this source. Short: PancakeSwap's custody now reads from `facts.py` again - `content/exchanges/pancakeswap.mdx` appeared on the site mid-session, so the hand-written literal from v3 came back out. **This pair also went through the full post-upload pipeline for the first time**: `youtube-audit set` for title/description/tags, `render_video_poster` (new - a straight format change for the long, a blurred-letterbox for the short) into `videos.json` on both, and the Share Video n8n workflow to Facebook and Telegram for both. Recorded as a standing workflow in the skills, not just done once. |
| `crypto-exchanges-short` v3 | `posts/understanding-crypto-exchanges` | 44.8s | `mia` (candidate) | Eleven shots. The lineup moved from a 2x2 to **one column under a heading per group** — `CENTRALIZED` over Coinbase and Binance, `DECENTRALIZED` over Uniswap and PancakeSwap — because two crosses and two ticks in a grid still left the viewer inferring what the sides meant. The heading arrives with its own first tile rather than at f=0, same reasoning as `compare(name_columns=True)`. PancakeSwap replaces Hyperliquid now the user has added `exchanges/pancakeswap.webp`; there is still no `content/exchanges/pancakeswap.mdx`, so its verdict is the one row on screen not read from `facts.py` and is written as a statement about the mechanism (an AMM swaps from the user's own wallet) rather than an opinion. **The verdict badges had never once worked in a built short** — `logos` was added with tick/cross support and the mark times were only ever computed for `checklist`, so `marks` stayed None and every badge silently did not draw; correct in an isolated render, absent in the video. Fixed in both build paths, and the cue builder now reads the verdict as the item's last element because a checklist row is `(text, ok)` and a logo tile is `(slug, label, ok)`. Also: "Always remember the golden rule." now hands off into the full-screen card, and the closing question uses a hyphen — em and en dashes are banned in any on-screen or spoken line from here. |
| `crypto-exchanges-short` v2 | `posts/understanding-crypto-exchanges` | 44.0s | `mia` (candidate) | Ten shots. Narrows the long form's angle rather than repeating it: it names four real platforms and answers the custody question for each, off `facts.py` reading the site's own `quickFacts`. **v2 rebuilt the beat.** v1 used a four-row checklist with three crosses and one tick and the user found it confusing — with a single tick at the end nothing says the tick means *decentralized* rather than "the best one". Now a 2x2 of the site's own brand cards, two labelled "they hold" and two "you hold", with the verdict badge landing into each tile's corner in the pause; balanced sides make the split the subject. **PancakeSwap was asked for and is not in the library**, so `hyperliquid` — the site's own second non-custodial exchange, with a page and real `quickFacts` — takes the fourth slot. Closes on a full-screen `chapter` card at 148px rather than a caption over footage. Also fixed: `picture=` is silently ignored in 9:16 (the vertical checklist wants `backdrop=`) so v1 drew its beat on flat black, the three habits gained the sentence saying why they are there, and the thumbnail moved to `band="bottom"` because the default printed the headline across the subject's face. |
| `crypto-satoshi-proof` v2 | same | 34.75s | `sam` (candidate) | Nine shots. Captions moved **under** the photograph — over it they covered the subject and left the lower third empty; `build.py` reads each shot's `photo_box(0.5)` and sets that sentence's caption y from it. Checklist reworked: white ink throughout (grey-on-dark was not readable on a phone), drawn over a dimmed photo at 0.5 rather than flat black, and items now reveal on their own caption starts instead of even fractions of the shot. Script extended with the verification step and a closing question for comments; two emoji, via `add_caption_emoji` moved from tinnitus into `core.vertical`. -14.9 LUFS. |
| `crypto-satoshi-proof` v1 | `what-it-actually-takes-to-prove-someone-is-satoshi-nakamoto` | 23.45s | `theo` (candidate) | First build of the format. Seven shots, one per sentence, cut to measured narration. Picture is the site's own images in a blurred-fill layout: the source scaled to cover, blurred and dimmed to 42%, sharp copy over it, nothing upscaled past 1.45. Sources are 700-1200px and all landscape, so each is cropped toward a 1.15 aspect first — at full width a landscape image is a third of a 9:16 frame and the rest is blur. `crypto-ogs/satoshi.png` (400px) was swapped for `posts/hacker.jpg` (996px) because below ~750px the upscale cap leaves the photo floating in blur. Sixth beat is a drawn checklist rather than a photo — the tick and cross are line segments because Futura renders both glyphs as tofu. Captions at `y_frac=0.70`; centred type lands on faces. Watermark is the site logo, upper-left, levitating. -15.0 LUFS, -1.5 dBTP. |

## Tinnitus Help — shorts

| video | format | runtime | voice | notes |
|---|---|---|---|---|
| `tinnitus-breathe-60` | ASMR / sound therapy | 60.93s | `luna-calm` (new, still a candidate) | Approved as a first cut. First build of the format — nothing past the voices existed before it. Bed layers both source tracks because measurement said one is not enough: SpaceshipAmbience puts 86.8% of its energy under 200Hz and 0.1% above 4kHz, NebulaPulse carries the mids. Ducking is a sidechain at `threshold=0.03:ratio=8` over a `-23 LUFS` bed; at `-20` the voice had only 3 dB of headroom and was not intelligible. Picture is a procedural nebula in the app's own palette with a paced-breathing ring, 4s in / 6s out, three cycles. **The drift must be subpixel** — the first cut used an integer crop, moved ~25px/s, and the user called it laggy; `warpAffine` fixed it and a frame-difference check now shows no two consecutive frames identical. Angle is partial masking, from the brown-noise-vs-white-noise post. No lead-in silence and no end-card promo, both the user's call. Watermark is a mascot + `TinnitusHelp.me` lockup at (58, 292), full opacity, levitating 9px on a 5.5s sine (subpixel, or it stutters against a background that does not). Three placements before it landed: flush top-left corner sits under TikTok's LIVE button and Instagram's camera, dead centre read as part of the piece, inset upper-left is a watermark. Safe box recorded as `SAFE_TOP=230` / `SAFE_BOTTOM=1440` and `render_visual` now raises rather than shipping outside it. Headphones emoji set inline after "headphones on." via a second pass over the caption PNG — Apple Color Emoji only loads at its strike sizes, so it renders at 160 and scales down. |

## Shorts

| video | clip | runtime | voice | text | notes |
|---|---|---|---|---|---|
| Akutino — "the sky will clear" (reveal format, stacked) | Coast 5 retimed 0.82x on top, `crop=2621:2160:480:0`; Forest Coast Houses below, `crop=2621:2160:1152:0`, start 2.0s | 22.03s | `leo`, aligned sentences, `gap=[0.6, 0.6, 0.9, 2.2]` | Iowan Old Style Italic 44px, stroke 4, `y_frac=0.50`, `tail=1.2`, turn word `ANYWAY` at 88px in `(255,176,72)` sampled off the sun track, kicker shown on screen | **The quote came from the user whole, kicker included** — "this is not about the weather, btw" was already the last line, so the reveal format fit it rather than being imposed. **Narration outran the footage**: 22.03s against Coast 5's 18.53s, and `render_narrated_stack` cuts both tiles to the full length, so it would have truncated silently. Retimed Coast 5 with `setpts` instead of cutting the quote — the clause at risk was the payoff, and 0.82x on a `pull_back` is invisible. **The dramatic gap belongs before the kicker, not after the turn word.** v1 put 1.7s after `ANYWAY` and the reveal landed too soon; v2 holds 2.2s on "to see the light return." and gives `ANYWAY` 0.9s. Two phonemization traps caught before rendering, not after: `read` phonemizes present-tense (`ɹˈiːd`) and `btw` comes out "bee-tee-double-you" — both fixed in the spoken half only. **Uploaded narration-only**, on the user's call, so the 2.2s hold plays as real silence rather than under music. |
| Cingene — "cold water" (reveal format) | Sunset Sea 2, `crop=838:1490:1891:384` (zoom 1.45), start 15.0s | 14.23s | `leo`, aligned sentences, `gap=[0.65, 1.9, 0.65]` | Futura Medium 44px, stroke 4, `y_frac=0.50`, `tail=1.2`, punchline `COLD` at 88px, kicker shown on screen | Three approved changes came out of this one. **No fade to black** anywhere in the shorts pipeline — a looping short should end on picture, not on an announcement that it is over. **`tail` 2.2 → 1.2** so the loop comes back faster. **`font_size` may now be a callable**, which sets the turn word larger than the body; `COLD` at 88px was chosen over the same word at 44px. The kicker is a normal caption here rather than the empty-caption treatment, at the user's request. `pick_crop` wanted x=2808 — the dense tree on the right outscored the sun — so the box is manual again. Text centred at 0.50 after 0.64 was rejected as "lower half". |
| Cingene — "plant a tree" (reveal format) | Forest Coast Reveal 1, `crop=1215:2160:1512:0` (zoom 1.0), start 1.0s | 16.32s | `leo`, aligned sentences, `gap=[0.65, 1.9, 0.65]` | Futura Medium 44px, stroke 4, `y_frac=0.48`, `tail=2.2`, kicker is an empty caption | First short in the reveal format: opener, metaphor, then "this is not about trees btw" spoken but never shown. One continuous clip — the camera tilts off the canopy to open sea by itself, so the reveal lands with no tree in frame; a three-clip version was cutting to manufacture what this clip already did. 0.18s of headroom against the 17.5s source, so ffprobe mattered. Background music added by hand afterwards. |
| Cingene — "self-care" | Sunset Sea 5, `crop=838:1490:1632:624` (zoom 1.45), start 8.0s | 10.55s | `leo`, aligned sentences | Futura Medium 44px, stroke 4, `y_frac=0.64`, `gap=0.65`, `tail=2.2` | First short in `leo`. Approved as the plain-comma variant of a three-way punctuation test — em dashes and ellipses were measurably negligible and lost by ear. A `hover` clip, so framing does not drift and one sampled frame sets `y_frac` for the whole run. Quote is Katie Reed's; the "i read a quote that said" opener carries the attribution. |
| City 1 — "ordinary tuesday" | City 1, `crop=1215:2160:0:0`, start 2.0s | 10.91s | `am_onyx` 0.60 + `am_puck` 0.40, melancholic, aligned sentences | Futura Medium 44px, stroke 4, `y_frac=0.34`, `gap=0.65`, `tail=2.2` | First approved narrated short. Voice is a blend — straight `am_onyx` is graded D on minutes of data; the blend sounded more human across ten candidates. Crop is manual: `pick_crop` chose rooftops at x=1776 and left the sun out. That voice was retired when `leo` was approved; the recipe survives as the `melancholic` chain. |

## Cingene

| tag | cuts | mean shot | coverage | effects | notes |
|---|---|---|---|---|---|
| `cingene-v7` | 50 + 3 head | 5.02s | 84% | 5 sped up, 0 escalates, 1 reversed | **Forest Coast 1 removed from the film** — `CLIP_MAX_USES = 0`, 14 assets instead of 15. It is the one clip grading could not have rescued: a* -2.0 (the only clip on the green side of neutral), hue 126deg against a 52deg cluster, L50 72.2 against a median 39.6, and highlights **clipped** at L99 100.0, which is detail that does not come back. **It cost 39.7s and there is no slack left.** Capping Forest Coast 2 to 5 as well leaves the edit 29s short; MAX_USES_DEFAULT at 6 leaves it 35s short. Three clips now run at the 7-slot ceiling — the price of removing a clip this size, paid knowingly. **First minute rebuilt**: Sunset Sea Houses pinned at 0:40.5 and Sunset Sea Boats 1 at 0:57.9, which leaves exactly one Forest Coast 2 in the first minute at 0:43.4. Removing Forest Coast 1 had already pulled Houses into the opening on its own, but at bar 16, two shots from where a second Houses then wanted to sit; pinning it at bar 14 uses the reservation rule to keep it out of the earlier slot instead. **The music did not need re-syncing.** The remaining clips absorbed Forest Coast 1's six bars, so the timeline is still 92 bars / 4:26 and the bar 52 -> bar 40 loop is untouched — still the 0.93 to 0.93 exact energy match. |
| `cingene-v6` | 46 + 3 head | 5.43s | 77% | 7 sped up, 0 escalates, 1 reversed | **4:26, 11.6s shorter, and the loop seam is an exact energy match for the first time.** Pass 1 hands off at bar 52 and pass 2 returns at bar 40 — both inside the closing peak at 0.93, bar 40 a section line. Searching every phrase-line pair in the 89-93 bar range, only two give a 0.00 energy step and this is the one that keeps the arc: pass 1 delivers calm-peak-peak-build-into-the-big-peak, then steps back twelve bars inside that peak and plays out to the song's own ending. The old 40/24 seam was a 0.93 to 0.69 drop and audible as the music sagging at the join. **Sunset Sea Boats 2 moved into the first minute** at 0:23.2, whole, taking the Forest Coast 2 slot — which also removes one appearance of a clip that needs grading. **Forest Coast Reveal 1 lost its ramp**: 6 bars at 100%, 17.35s of a 17.5s clip, opening the build at 1:32.6. The launch was an effect on a shot that did not want one. **`SPEEDUP_MIN_REMAINING` 15 -> 30.** The doubled peak (bars 40-68 after the loop) is 28 bars of high energy, which generates many short slots, and speed-ups are favoured there: at 15 the edit put **18 shots at 2x**, halved the timeline covered per second of source and ran dry 26s short. Seven is the right number. **Colour notes rewritten from measurement, not from the index summary** — twelve frames per clip sampled in CIELAB off the cached proxies. The golden-hour cluster medians are L1 5.7 / L50 39.6 / a* +7.4 / b* +9.4, hue 51.9deg, chroma 12.0. Forest Coast 1: L50 **72.2** (-0.87 stops), L95 99.6 **clipped**, a* **-2.0** — the only clip on the green side of neutral — chroma 3.4 against 12.0. Forest Coast 2 is a different fault: L1 **9.8** against 5.7, a raised black floor that reads hazy and is what flattens its chroma; black point first, colour second. |
| `cingene-v5` | 47 + 3 head | — | — | 1 escalate, hand-cut head | **v3's timeline with the hand-cut opening on the front, and the whip bug fixed.** Everything v4 added — the reversed pair, the bar 20 seam, the 8-bar closer, the Forest Coast 1 cap change — is reverted; only the opening is kept. 50 spine clips, 277.67s. **The whip bug was mine.** The writer forced `start="0s"` on any clip carrying a timeMap, which is right for a generated clip (the in-point lives in the first timept, and setting both is the "Invalid edit with no respective media" trap) and wrong for a captured one. Final Cut writes the other arrangement for a trimmed retime: the curve describes the whole retime from source 0, and `start` is the in-point INTO the retimed clip. Zeroing it made the 4x pass replay Sunset Sea 1 from the top instead of continuing through it at 0:21. `start` is now passed through for captured clips, and the validator flags only the case where both carry an in-point. **`charge` on a HEAD_CLIPS entry** states how far the cursor moves, overriding what the entry consumed. The head plays 17.1s of Sunset Sea 1 and whips back over seconds it already showed — deliberate — but charging that honestly pushed the clip's three later slots forward and silently re-cut the approved timeline under a change meant to affect only the opening. Pinned at 5.8s, where the two bars it replaced left it. Footage moved to `~/Movies/Cingene`: macOS revoked this session's access to Desktop, Documents and Downloads mid-project. The index keeps absolute paths and `drone index` will not rewrite a cached row, so the paths were updated in `clip_index.sqlite` directly — a build validated clean while every asset pointed at a file it could no longer see. |
| `cingene-v4` | 46 + 3 head | 5.90s | 84% | 2 sped up, 0 escalates, 3 reversed | **The hand-cut opening is now part of the build.** Three clips captured from a Final Cut export and replayed verbatim: Sunset Sea 7 for 12.433s, Sunset Sea 1 for 8.667s, then 2.1s of Sunset Sea 1 on a smoothed 4x whip landing on the drop at 23.200s. `HEAD_CLIPS` + `HEAD_UNTIL_BAR` — the generator cuts on bar lines, a person cutting the head by ear does not, and on the head by ear is better. `build` overwrites the FCPXML wholesale, so without this the work had nowhere to live. **The grid was four frames late.** Bar 8 computed to 23.318s against a drop at 23.200s. Downbeat confidence on this track is 0.39, the lowest in the repo, and this is exactly what that warns about: a grid that scores well and feels late. `GRID_PHASE_NUDGE = -0.133333` moves every cut by the same amount and is what makes the hand-cut head join the generated spine with no gap. **Reversed pair at 0:23-0:43**: Forest Coast Reveal 1 at 2x (whole clip) then Forest Coast 2 at 2x from 6.0s, both backwards — coast to forest, then sea to houses to trees, one continuous climb inland. Reveal 1 left its 1:44 ramp to do it, so `ESCALATE_AT_BARS` is empty. Loop return bar 24 -> 20 for the seam (0.93 -> 0.86, the join that sounded right); 100 bars, 4:49. **Four engine changes, all forced by real failures.** The lock carries `reverse` per slot — Forest Coast 2 has one backwards slot out of three and `CLIP_REVERSE_OVERRIDE` can only flip a whole clip. `dump_lock` skips head clips, which were otherwise written back as zero-length bar-0 slots and emitted twice. Captured timeMaps are stored in frames, not decimal seconds, which had been rounding a hair past the asset duration. The validator's timeMap span check now allows a curve that runs past the clip: FCP writes exactly that for a trimmed retime, so the old check rejected a fragment FCP itself authored — and head clips are exempt from the repeated-source check, since a whip racing back over what it just played is a choice there. `interp` is passed through for captured curves and stays linear for generated ones. Forest Coast 1 back to 6 uses from 4: the head spends Sunset Sea 1 and Sunset Sea 7 outright and the reversed pair takes 23s of Forest Coast 2, and at 4 the edit stopped 40s short with footage stranded behind the cap. Closer runs 8 bars at 1x rather than 4 at 2x — same 23.14s of source, twice the timeline, which is the difference between reaching the song's ending and not. |
| `cingene-v3` | 49 | 5.67s | 82% | 6 sped up, 1 escalate, 1 reversed | **35s shorter and much flatter in clip usage.** Loop return moved bar 12 -> bar 24, so 96 bars / 4:37 instead of 108 / 5:21; both ends of the seam are section and phrase lines, and pass 2 now climbs back through the build into the 0.93 peak, giving a two-peak arc. Usage went from 8/8/7/7/6/5/5/3/3/2/1/1/1/1 to 7/7/5/5/4/4/4/4/3/2/1/1/1/1 — the repetition complaint was distribution, not cursor position. **Sunset Sea 7 opens**, whole at real time over six bars; Forest Coast Reveal 1 moved to 1:44 and plays forward at 100% for ~9s then ramps over 2.6s, **fitted to 500%** against an ideal 550%. The 2x version of it read as a mistake, which is what the escalate is for: already slow, fast only at the exit. **Forest Coast 1 capped at 4 uses (was 7).** It is the only pre-golden-hour clip and the widest outlier in the batch on three axes at once — hue 111.5 against a 22.2 degree warm cluster median, saturation 0.185 against 0.343, 6.9% blown — so the clip needing the most hand grading was also the one on screen most. `CLIP_MAX_USES` turned out to be the right handle for a colour problem, not just a structural one. **Sunset Sea 3/4/5/6 trimmed head and tail** (1.0-2.0s off each end via `CLIP_HEAD_SKIP` plus a tail `CLIP_SKIP_RANGES`) and `SPEEDUP_MIN_REMAINING` dropped 25 -> 15 so they can run 2x. They are static holds on open water where one second is much like another; the clips with houses, boats, trees or a reveal in them are untouched. Colour notes in `cingene-colour.md`: exposure spread is only 0.80 stops and is not the problem — the two hue outliers are, and Sunset Sea 7 is now one of them by virtue of opening the film for 17.5s. |
| `cingene-v2` | 58 | 5.39s | 86% | 3 sped up, 0 escalates, 1 reversed, no overlay | **The four reveals are now single uncut shots.** Forest Coast Reveal 1 and 2, Sunset Sea 7 and Sunset Sea Boats 2 are each one continuous move from an overhead frame down into the sunset; split across slots the gesture is gone and the halves read as two ordinary shots. Opener is Reveal 1 whole at 1.962x over three bars, closer is Reveal 2 reversed at 2x over the last four — sunset back to the forest the video opened in. Location pin dropped. **Three engine changes came out of this, all general.** `CLIP_MAX_USES` caps a named clip, because move type is the wrong handle for "show this one whole" — capping every vertical to reserve one starves the library. It is the only cap the relax pass will not lift, since lifting it would undo the one thing it was for. **A pin is now also a reservation**: without that the scorer spent each capped clip on an earlier slot on merit and the pin then reported "no material left", which is guaranteed when one use is all there is. **A pinned slot length is taken literally** — not filtered against LEGAL_SLOT_BARS or MAX_SHOT_SECONDS. Three bars and six bars are lengths the scorer would never choose, and they are the ones that fit a 17.5s reveal at 2x and a 17.7s reveal at 1x. `_absorb_lead_in` now fits the rate down instead of leaving a gap: at 2x the five-frame lead-in before the first bar line costs ten source frames, which a clip chosen to fill its slot exactly does not have — hence 1.962x, not 2x, and it is reported. Also `HEAD_TRIM = 0.0` for this project (the 0.3s settle-in plus 0.1s tail cost 0.4s, and three of the four reveals fail by fractions of a second with it on), and the loop return moved from bar 8 to bar 12 — 108 bars instead of 112 — because the closer needs the timeline to end where the footage does. Cooldown back to the 4/12 defaults; at 6/16 the tail fragments were all cooling and the edit stopped 23s short with 76s unused. |
| `cingene-v1` | 69 | 4.70s | 85% | 2 sped up, 0 escalates, 7 reversed, 1 pin overlay | First edit of the fishing village south of Burgas. 14 selects, 393s of source. **The index held two ghosts** — `Forest Above.mp4` and `Forest Coast Reveal 3.mp4`, rows for files no longer on disk; left in place the scorer would have assigned missing media, so they were deleted from `clip_index.sqlite`. Track is 232.1s / 80 bars at 82.99 BPM (bar 2.892s) and straight through used only 70% of the library, so it is looped once: hand-off at bar 40, the downbeat of the closing peak (0.93), returning at bar 8, the downbeat of the opening peak (0.86) — the closest-matched pair in the track, both section and phrase lines. 112 bars, 5:24, and pass 2 still plays the song out to its real ending. Requested order pinned: Forest Coast Reveal 1 opens at real time (17.5s of source under an 11.7s slot), Sunset Sea 1 second, Sunset Sea 2 at 0:34 with four shots between them so the two sunsets read as a transition rather than a pair. **The closer could not be pinned.** Forcing a slot boundary at bar 108 for Forest Coast Reveal 2 made the engine drop the final slot outright: a pin cannot invent footage and 393s under a 324s timeline leaves no spare. Capping vertical uses to hold source back was worse — the library ran dry at bar 100 and the timeline collapsed to 4:49. Set in the lock instead, which is what the lock is for. Even then 2x needed source freeing: eight slots on a 28.1s clip left ~6s at the end, the build fitted the shortfall into the timeMap and produced a **0.28x segment** that the validator rejected. Swapping the bar 80 use to Sunset Sea Houses (58.7s, the only clip with room) funded the full 2x. `CLIP_REVERSE_OVERRIDE` forces reverse on every use of Reveal 2 — it was already auto-reversed in most slots and mixing both directions of one shot reads as a mistake. Downbeat confidence is 0.39, the lowest so far, so a click track was written for a listen-through. |

## Nessebar

| tag | cuts | mean shot | coverage | effects | notes |
|---|---|---|---|---|---|
| `nessebar-v6` **shipped** | 31 | 7.74s | 82% | 0 sped up, 1 escalate, 5 reversed, 3 SFX | **The version to use.** Picture is byte-identical to v5; what changed is the asset uids, without which Final Cut refuses the whole document. Tag v5 alone does not import. The intro-burst variant was built and reviewed at this point and set aside — kept as `nessebar-burst` for testing on a future video, not a rejection. |
| `nessebar-v5` | 31 | 7.74s | 82% | 0 sped up, 1 escalate, 5 reversed, 3 SFX | Title re-captured after a hand fix — the authored version rendered solid black for two reasons the DTD cannot express: **strokeWidth must be negative** for an outside stroke (positive strokes inward and eats the fill), and the **`MotionSimpleValues` param block carrying the face fill must be present**. Attribute *names* are DTD-declared and safe to author; these values are not. `SOUND_EFFECTS` carries hand-placed audio through rebuilds — three seagull placements recorded, since the build overwrites the FCPXML wholesale and anything added by hand in FCP would otherwise be lost. Also strips `<adjust-colorConform>` (an FCPXML 1.11 element FCP adds to everything, which fails the 1.10 DTD) and narrows the connected-clip sync check, which was flagging correctly-placed SFX. |
| `nessebar-v4` | 31 | 7.74s | 82% | 0 sped up, 1 escalate, 5 reversed | Title styled — white, dark outline, widened kerning, via `LOCATION_TITLE_STYLE`. Every key is a declared FCPXML text-style attribute per the 1.10 DTD, so this is ordinary authoring, not the guess-a-uid trap; only strokeWidth's sign convention is unverified. Ends on Coast Above 2 running out to the last frame of its source (39.8→49.4s of 49.9s), closing Coast 2 dropped. Coast Houses Above 2 off 2x at 0:57 — no constant speed-ups left in the video. The 2:27 escalate re-fitted itself to 100%→1600% to fund the longer ending, which is `owed_after` working as intended. |
| `nessebar-v3` | 32 | 7.50s | 84% | 1 sped up, 1 escalate, 5 reversed | Title text beside the pin, captured from a user FCP export and committed to `assets/fcpxml/location-title-overlay.xml` — the Basic Title uid and its three `param key` paths are FCP-internal, same class as the keyer. Coast Above 2 doubled to 12.8s at 0:38 (Coast Above 1 moved to 3:37 to fund it). Coast 1 plays straight at 100% with its first 2s cut, no ramp. Coast Houses Above 2 un-reversed via `CLIP_REVERSE_OVERRIDE` — AUTO_REVERSE had it driving traffic backwards. **Fixed the escalate budget double-count**: `build_locked` charged a clip for every other slot including ones already consumed, so lengthening an earlier shot silently starved a later ramp. The 2:27 escalate now reaches its full 100%→2000%. |
| `nessebar-v2` | 34 | 7.06s | 86% | 1 sped up, 2 escalates, 8 reversed | Locked. **Escalate body speeds are now whole multiples only** — 160% stutters because 1.6 source frames per timeline frame forces an uneven repeat/drop cycle; confirmed on Coast 1, smooth at 100% and 200%, laggy at 160%. `fit_escalate` stepped 0.1 at a time hunting the fastest fundable profile, which is how 160%/150% got in. Both ramps now run a 100% body: Coast 1 at 1:10 → 1000%, Coast Above 2 at 2:27 → 2000%. Also: location pin overlay at 1.5s; `CLIP_SKIP_RANGES` excludes Coast Above 3's 6.7–9.9s off-angle rotation; Coast 1 reduced to a single ramped shot; first minute rebuilt as a showcase (8 distinct clips, all three Coast Above at 6.4s each) because it is cut for shorts; Coast 4 pulled to 1:04, Coast 3 pushed to 2:56. Title text next to the pin still outstanding — needs a generator UID captured from a real FCP export. |
| `nessebar-v1` | 38 | 6.32s | 83% | 3 sped up, 9 reversed, 0 repeated ranges | First edit on a correctly-detected grid. **librosa reported 152.11 BPM for a 75.00 BPM track**, and the least-squares fit made that look clean — off-beat onsets measured *stronger* than on-beat. Tempo is now found by direct (period, phase) search scored on onset energy: wrong grid scores 0.008, right grid 2.296; downbeat confidence 0.08 → 0.53. Track looped once (hands off 2:08.1, re-enters 0:38.5, 6.4s crossfade, both phrase lines, energy 0.93 vs 0.90) to carry a 2:36 song over a 4:00 picture. Approved on cuts and on the loop seam. |

### History

- **v0** — built on the 152 BPM misread: 53 cuts, mean 2.94s, 63% coverage, cuts
  landing on a grid unrelated to the music. Superseded, never tagged.

## Plovdiv

| tag | cuts | mean shot | coverage | effects | notes |
|---|---|---|---|---|---|
| `plovdiv-v7` | 47 | 4.81s | 74% | 12 sped up, 2 escalates, 4 reversed | City 4 dropped from 2x to 100% (reverse kept) — it read too fast over the city. Order byte-identical to v6. |
| `plovdiv-v6` | 47 | 4.81s | 81% | 16 sped up, 2 escalates, 4 reversed | Locked timeline — opening restored to v4's order (City 1 first). Hill Tower's overexposed head skipped, cascading its remaining uses forward. Second escalate before 1:00, fitted to 160%→403% (footage-limited). |
| `plovdiv-v5` | 47 | 4.81s | 81% | 16 sped up, 1 escalate, 4 reversed | Phrase-snapped sections: 2:11 and 2:21 now on the beat. Pinned City 2 @0:32.83 and City 7 @0:37.87. Picture and music fade together 222.03→226.03s. Opener drifted to City 5 as a side effect — caught and fixed in v6. |
| `plovdiv-v4` | 49 | 4.61s | 83% | 14 sped up, 1 escalate, 5 reversed | Escalate on Hills Monument at 0:10–0:20: 200% body, 2000% final second. Order matches v3 through 2:28. |
| `plovdiv-v3` | 49 | 4.61s | 80% | 15 sped up, 2 punch ramps, 5 reversed | Opening order approved. Speed-up only, no slow motion. Zero repeated source ranges. |

### History

- **v1** — first working timeline. 62 cuts, mean 3.64s. Pacing liked; a
  60-second stretch of identical 2.50s cuts was not.
- **v2** — phrase accents broke up the metronomic run; reuse penalty changed
  from counting uses to decaying with recency, which stopped the engine
  degenerating into round-robin and putting hovers on the drop.
- **v3** — slow motion removed entirely after review (it read as a mistake on
  this footage); retiming became speed-up only. Punch ramps into peaks. Auto
  reverse for clips travelling the same direction. Coverage 59% → 80%.
- **v4** — the 1.0x-start punch ramp replaced by the escalate: 200% throughout,
  2000% across the last second, launching into the next clip. Placed explicitly
  via `ESCALATE_AT_BARS` rather than won by scoring, because a scored escalate
  took slots from other clips and reshuffled the running order.
- **v5** — `SNAP_SECTIONS_TO_PHRASE` turned on, fixing 2:11 and 2:21 at the cost
  of re-laying the slot grid (37 of 47 positions moved from v4). Added
  `PIN_CLIPS` / `PIN_SLOT_BARS` for hand-placing a clip in a slot, and a picture
  fade to black under the closing music fade. **Regression**: the opener shifted
  from City 1 to City 5 as an uncalled-out side effect of the re-lay.
- **v6** — the running order is now **locked** (`projects/plovdiv.lock.toml`):
  `build` replays the approved slot grid and clip assignment verbatim, so
  tuning elsewhere can no longer reshuffle it. The v5 opener regression is
  fixed — restored to v4's City 1. Hill Tower's overexposed first segment is
  skipped via `CLIP_HEAD_SKIP`, cascading each remaining use forward by one
  segment; the freed slot goes to unused footage. A second escalate added
  before the 1:00 cut, but the ideal 200%→2000% profile doesn't fit — City 11
  owes 22.5s to its later slots in the locked order, so the escalate fits
  itself down to whatever speed the spare footage can fund (160%→403% here)
  rather than borrowing from those slots and moving the timeline again.

- **v7** — City 4's four instances dropped from 2x to 100%, reverse retained.
  First change made purely by editing the lock file; the running order came out
  byte-identical, which is what the lock exists to guarantee.

### Colour notes (measured, not yet acted on)

The 15 clips split into two colour families rather than drifting: warm 10-79°
(ten clips) and cool 184-201° (City 11, 4, 3, 2), with City Above alone at 346°.
**20 of 46 cuts jump more than 120° in hue** and this was never flagged during
review — on a ~4.8s average shot length the warm/cool alternation reads as
energy, not error. Treated as taste, not a fault.

More visible than hue, and worth addressing first:

- **Brightness**: City Above (0.41) and Hill Tower (0.43) sit well under the
  0.56-0.70 cluster — roughly a 1.5-stop step at some cuts.
- **Saturation**: Hill Tower (0.59) is ~3x City 1 (0.18) and City 11 (0.19),
  so it reads as the most processed shot whenever it appears.
- **City 11's 28.3% blown highlights are NOT recoverable.** The clipping is the
  sunset cloud, uniform across the whole clip (27.5-29.0% in every 5s window),
  and these are graded exports — pixels at 255 have nothing behind them. What
  *is* fixable there is haze: City 11 measures 186 on detail density, the lowest
  of all 15 and ~10x below the sharpest.

### Known open items

- **3:22 lands 1.07s early.** The accent sits at bar 80.42 — roughly beat 3 —
  and the engine cuts only on bar lines, so 3:20.93 and 3:23.44 are the only
  reachable points. Needs slot lengths expressed in beats.
- **The 1:00 escalate is capped at 403%, not 2000%**, because City 11 is
  committed to three later slots in the locked order. Getting the full ramp
  there means freeing one of those slots (moves the timeline) or choosing a
  different, less-committed clip for that launch.
- **Slice selection is sequential**, so the back half of the longest takes is
  reached only through reuse. Spreading each clip's slices across its full
  length is the obvious next improvement.
- **Editing the timeline now means editing `plovdiv.lock.toml` directly**
  (swap a `clip` value, adjust `bars`, or set `rate`) rather than retuning
  `config.py` weights — that's the point of the lock, but worth remembering so
  a future request for "more variety" or "less repetition" isn't chased through
  scoring parameters that no longer apply to this project.
