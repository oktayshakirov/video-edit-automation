# thecrypto.wiki quiz shorts

One script per video, built by `/video-quiz crypto`. Each sets `SOURCE_POST` to
the article its questions come from, so `tools/topics.py crypto --quiz` can see
what already has a quiz.

The format, its limits and the compliance line are in
`docs/video/projects/quiz.md`. Read it before writing questions, not after.
